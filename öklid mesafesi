import math

def euclideanDistance(point1, point2):
    x1, y1 = point1
    x2, y2 = point2
    return math.sqrt((x2 - x1)**2 + (y2 - y1)**2)

pointA = (1, 2)
pointB = (4, 6)

distance = euclideanDistance(pointA, pointB)
print(f"{pointA} ve {pointB} arasındaki mesafe: {distance}")
